# OPNsense peer enrollment

Use [the operator runbook](../../../docs/operations/opnsense-netbird-enrollment.md).
It covers compatibility, local recovery, backups, bounded plugin settings,
one-use enrollment, checks, and rollback.

The runbook is a draft until the role and enrollment command exist. Enrollment
must leave North York LAN routing inactive.
