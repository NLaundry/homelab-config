# NetBird control-plane adoption

Use [the operator runbook](../../../docs/operations/netbird-opentofu.md).
It covers import review, encrypted external state, the unrouted baseline, checks,
and recovery.

The runbook is a draft until the OpenTofu root and adapter exist. Do not guess
import addresses or activate routing during adoption.
