## 1. Current instrument truth

- [x] 1.1 Add `tests/agents/specbase-instruments.sh` with independent `config`, `validate`, and `lenses` modes and actionable failures.
- [x] 1.2 Make `config` assert `specbase/config.yaml` declares the governed schema and exact configured plane IDs without depending on key order.
- [x] 1.3 Make `lenses` compare stable lens IDs and scopes in `.pi/skills/specbase-review-panel/SKILL.md` with the resolved lens set from `specbase coverage --json`.
- [x] 1.4 Remove remaining OpenSpec/opsx names and paths from the two current agents pairs when the change is archived, while preserving stable normative IDs.

## 2. Paired evidence

- [x] 2.1 Run each conformance-script mode and demonstrate that a controlled wrong schema, missing lens, and mismatched scope are detected.
- [x] 2.2 Run strict project spec validation and coverage after the current paths are in place.
- [x] 2.3 Promote compact bindings for `specbase-validates`, `governed-schema-declared`, and `lens-roster-conforms` after their shared source exists and passes its native modes.

## 3. Cleanup and validation

- [x] 3.1 Confirm the retired `openspec-validates` and `panel-review` bindings have no surviving consumers before removing them through the enforcement deltas.
- [x] 3.2 Confirm coverage reports no broken targets, hanging requirements, uncovered scenarios, or unlensed review binding for either agents pair.
- [x] 3.3 Run strict validation for `repair-specbase-agent-drift` and record the green project state needed by `establish-testing-operations`.

## 4. Compact enforcement migration

- [x] 4.1 Add selector-addressable executable and manual sources that preserve every legacy binding's evidence mechanism and strength.
- [x] 4.2 Migrate all eight non-agent current pairs to compact requirement-level `enforcement.yaml` indexes and add missing Purpose sections without changing normative IDs.
- [x] 4.3 Execute every workstation-safe binding selector; retain live SMB, boot, SSH, and deployment outcomes as environment-dependent or manual evidence.
- [x] 4.4 Simulate pair-aware archive and confirm all ten promoted pairs pass strict validation and coverage before archiving the real change.
