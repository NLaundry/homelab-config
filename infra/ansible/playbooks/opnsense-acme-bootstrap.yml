---
- name: Bootstrap North York native ACME
  hosts: opnsense
  connection: local
  gather_facts: false
  no_log: true
  vars_files:
    - ../vars/north-york.yml
  module_defaults:
    group/oxlorg.opnsense.all: "{{ opnsense_module_defaults }}"
  pre_tasks:
    - ansible.builtin.import_tasks: ../tasks/opnsense-connection.yml
  tasks:
    - name: Run the ACME bootstrap tasks
      ansible.builtin.include_role:
        name: ../roles/opnsense_acme
        tasks_from: bootstrap
