# introduce-typed-estate-registry

Add a minimal typed Nix Estate registry for sites, hosts, workloads, state ownership, placement, dependencies, and failure domains, with evaluated graph properties and diagnostic diffs.
