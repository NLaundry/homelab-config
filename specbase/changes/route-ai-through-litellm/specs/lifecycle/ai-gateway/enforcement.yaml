bindings:
  ai-gateway-transitions:
    type: test
    covers:
      - guest-autostart
      - failed-service-restart
    source: tests/ai-gateway-vm.nix
  deployed-ai-health:
    type: test
    covers: activation-health
    source: tests/verify/ai-gateway-health.bats
