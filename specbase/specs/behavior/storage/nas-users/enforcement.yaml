bindings:
  operator-config-intent:
    type: command
    covers: operator-access
    source: tests/specbase/current-bindings.sh#operator-config-intent
  operator-ssh-sudo-runtime:
    type: manual
    covers: operator-access
    source: tests/specbase/manual-verification.md#operator-ssh-sudo-runtime
