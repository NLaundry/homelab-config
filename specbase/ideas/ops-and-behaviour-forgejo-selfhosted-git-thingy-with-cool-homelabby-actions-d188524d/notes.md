Might be good for handling actions and CI stuff

I think I'll want a separate host for OPSy things like running my CI.

Reason: testing things like if samba shares are actually detectable requires a separate host to really work. Same with network discoverability in general. I just want this device to be separate from the main "service hosting" devices.
